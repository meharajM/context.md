# `android/gradle/wrapper` Architecture

Gradle wrapper files.

## Files

- `gradle-wrapper.properties`: wrapper distribution configuration.
- `gradle-wrapper.jar`: wrapper launcher binary.

Treat these as build toolchain files. Changes should be tied to an explicit Gradle/Android build upgrade.

