# `__tests__` Architecture

Top-level Jest tests for app-level behavior.

## Files

- `App.test.tsx`: app shell/render behavior.
- `mocks/fileMock.js`: static asset mock.

Module-specific tests live next to their modules or features under `src/**/__tests__`.
