# `scripts` Architecture

Reserved project scripts directory.

There are currently no active project scripts here. Before adding scripts, document their purpose, required environment, and whether they are safe to run in CI or only intended for local/manual use.

