# `src/screens` Architecture

Reserved screen directory.

The current app uses feature directories plus `src/app/AppShell.tsx` for screen composition, so this directory is currently empty. Prefer adding new screens under the relevant `src/features/<feature>` directory unless a future navigation refactor establishes this as the screen boundary.

