# `src/components` Architecture

Project-specific standalone components.

## Files

- `BrandMark.tsx`: reusable brand mark.

Keep this directory small. Components that are shared primitives should usually live in `src/shared/components`; components tied to a feature should live in `src/features/<feature>`.
