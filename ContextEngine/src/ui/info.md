# `src/ui` Architecture

This directory contains higher-level UI design constants for the current visual language.

## Files

- `design.ts`: app-level visual constants used by current components and screens.

Prefer `src/shared/design` for reusable tokens. Use this directory for app-specific visual composition that is not yet generalized.
